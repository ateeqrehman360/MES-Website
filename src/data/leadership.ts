export type Leader = {
  name: string;
  role: string;
  image: string;
  linkedinUrl?: string;
};

// Current leadership will be added from approved society information.
export const leadership: Leader[] = [];
